import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'faroese-language-app',
  webDir: 'www',
  server: {
    androidScheme: 'http'
  }
};

export default config;
